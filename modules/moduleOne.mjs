/* const sum = (a, b) => a + b

export default sum
 */

/* =========================================================================================================================================== */
//Експорт декількох елементів:
//Приклад 1:
/* const one = 1
const two = 'two'

export {
    one,
    two
} */


//Приклад 2:
const sum = (a, b) => a + b
const mult = (a, b) => a * b

export {
    sum,
    mult
}